# PRF-008 - PRD-TASK-022 Remediation

Result: `IMPLEMENTED_AND_VERIFIED`

Runtime product execution remained stopped. I did not requeue or execute `PRD-TASK-022`, did not execute any other product task, did not approve gates, did not resolve `PRD-DEC-002` or `PRD-DEC-003`, and did not modify frozen product plan `PRODUCT-PLAN-7b0342fb7fd5` v1.

## Remediation

Source-reference remediation:

- Preserved the attempt-2 operation source-reference correction exactly.
- `OP-003`: `BEAG-001`, `R17`, `R20`, `R21`, `R22`
- `OP-004`: `BEAG-001`, `R17`, `R20`, `R22`
- `OP-005`: `BEAG-001`, `R17`, `R22`
- Existing tests prove requirement IDs such as `OPS-004` are rejected as `source_refs`, valid source refs are preserved exactly, multiple source refs retain the expected semantics, and serialize/readback is deterministic.

Migration/runtime-operations remediation:

- `MigrationSafetyChecker.check()` now accepts `require_postgresql_dialect`.
- Active authority recognition remains narrow: an active `BootstrapStateAuthority` with `backend=postgresql` and `status=active` is recognized and no longer causes a false `control_plane_authority` failure.
- Production CLI migration checks now require a PostgreSQL SQLAlchemy dialect, preventing SQLite/local fallback from masquerading as production authority.
- Failure details now distinguish unknown authority, fallback/non-authoritative authority, and stale/inactive PostgreSQL authority.
- The fix does not suppress `control_plane_authority`; it tightens the authority state model.

Deterministic coverage/count/hash remediation:

- The C20 runtime semantic projection was already implemented with `runtime_requirements_for_capability`, derived from compiled normative manifest requirements and existing `canonical_bytes` infrastructure.
- Revalidated tests cover baseline normative requirements, one added normative requirement, provenance-only operation changes, stable ordering/canonicalization, exact runtime coverage, and repeated generation determinism.
- No competing serializer/hash system was introduced.

## Verification

Targeted PRR-008 completion verification:

- Source-reference semantics: PASS.
- Runtime operations: PASS.
- `MigrationSafetyChecker`: PASS.
- PostgreSQL authority detection: PASS.
- Deterministic coverage/count/hash: PASS.
- Docker/runtime isolation: PASS.
- Runtime-handoff/package construction: PASS.
- Targeted cluster command result: `44 passed in 11.11s`.

Mandatory verification after ENV-RESTORE-003:

- `./aient/bin/python scripts/preflight.py`: PASS.
- `./aient/bin/python -m pytest -q`: PASS, `575 passed in 274.58s`.
- `./aient/bin/python -m ruff check .`: PASS.
- `./aient/bin/python -m pyright`: PASS, `0 errors, 0 warnings, 0 informations`.
- `./aient/bin/python -m alembic current`: PASS, `0020 (head)`.

Environment authority:

- Docker daemon is reachable at `/var/run/docker.sock`.
- Existing `aient-postgres` is running on `127.0.0.1:5432`.
- Existing `aient-postgres-data` volume is preserved.
- `pg_isready`: `127.0.0.1:5432 - accepting connections`.
- `psql SELECT 1`: `1`.
- SQLAlchemy `SELECT 1`: `1`.
- Alembic current: `0020 (head)`.

## Security and Isolation

The remediation does not grant unrestricted Docker authority, unrelated container access, control-plane PostgreSQL mutation authority, host filesystem escape, unrelated workspace access, secret exposure, arbitrary Git authority, arbitrary Codex authority, gate approval authority, verification bypass, or LAN/public exposure.

Migration/runtime checks remain observational unless explicitly authorized. Cleanup remains dry-run by default and requires explicit operator confirmation before mutation.

## Runtime Integrity

Live runtime state was re-read after verification. PRF-008 did not perform any product task execution, requeue, lease creation, gate approval, decision resolution, or frozen-plan modification.

Authoritative state remains:

- `PRD-TASK-022`: `blocked`
- attempts: `[1, 2]`
- next attempt: `3`
- active leases: `0`
- nonterminal executions: `0`
- completed: `17/25`
- remaining: `8/25`
- `PRD-DEC-002`: unresolved
- `PRD-DEC-003`: unresolved
- PostgreSQL authority: active
- recovery: `READY_TO_CONTINUE / BETWEEN_TASKS`

## Recommendation

Proceed to independent `PRV-010` before any `PRQ`/`PRE` action. Do not requeue or execute `PRD-TASK-022` from this PRF-008 pass.
