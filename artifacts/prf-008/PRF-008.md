# PRF-008 - PRD-TASK-022 Remediation

Result: `IMPLEMENTED_AND_VERIFIED`

Runtime product execution remained stopped. I did not requeue or execute `PRD-TASK-022`, did not execute another product task, did not approve gates, did not resolve `PRD-DEC-002` or `PRD-DEC-003`, and did not modify the frozen product plan.

## Remediation

Source-reference remediation:

- Added PRD-TASK-022 operations `OP-003`, `OP-004`, and `OP-005`.
- Preserved attempt-2 source refs exactly:
  - `OP-003`: `BEAG-001`, `R17`, `R20`, `R21`, `R22`
  - `OP-004`: `BEAG-001`, `R17`, `R20`, `R22`
  - `OP-005`: `BEAG-001`, `R17`, `R22`
- Added tests proving requirement IDs are rejected as `source_refs`, valid source refs are preserved exactly, and serialize/readback is deterministic.

Runtime-operations remediation:

- Added `RuntimeCleanupService`, `MigrationSafetyChecker`, backup guidance, and graceful shutdown handling.
- Fixed `MigrationSafetyChecker` to inspect schema through the active `Session` connection and to resolve authority through `BootstrapRunRepository.get_active_authority`.
- Real PostgreSQL migration check now reports:
  - `PASS schema_tables`
  - `PASS control_plane_authority`
  - `PASS human_gates`
  - `PASS independent_verification`

Deterministic coverage remediation:

- Replaced the stale hardcoded C20 runtime requirement tuple with `runtime_requirements_for_capability`, derived from the canonical manifest and existing `canonical_bytes` infrastructure.
- The authoritative C20 projection now includes `DATA-004`, `OPS-004`, `OPS-005`, and `OPS-006`.
- Acceptance tests now compare complete coverage against the semantic summary rather than a stale fixed count.

## Security and Isolation

The implementation does not grant unrestricted Docker authority, unrelated container access, control-plane PostgreSQL mutation authority, host filesystem escape, unrelated workspace access, secret exposure, arbitrary Git authority, arbitrary Codex authority, gate approval authority, verification bypass, or LAN/public exposure.

Cleanup is dry-run by default. Applying cleanup requires explicit operator confirmation. Backup guidance renders environment-variable references, not secret values.

## Verification

Targeted verification:

- PRR failure set: `20 passed in 6.90s`
- Nearby suites: `124 passed in 12.59s`
- Manifest validation: passed; `requirements_count=28`
- Maintenance migration-check: passed
- Maintenance cleanup dry-run: passed; `actions=0`
- Maintenance backup guidance: passed; no secret values rendered

Mandatory verification:

- `./aient/bin/python scripts/preflight.py`: PASS
- `./aient/bin/python -m pytest -q`: `566 passed, 6 skipped in 74.15s`
- `./aient/bin/python -m ruff check .`: PASS
- `./aient/bin/python -m pyright`: PASS
- `./aient/bin/python -m alembic current`: `0020 (head)`

## Runtime Integrity

Post-remediation authoritative state:

- `PRD-TASK-022`: `blocked`
- attempts: `[1, 2]`
- next attempt: `3`
- READY: none
- active leases: `0`
- nonterminal executions: `0`
- completed: `17/25`
- remaining: `8/25`
- `PRD-DEC-002`: unresolved
- `PRD-DEC-003`: unresolved
- PostgreSQL authority: active

## Recommendation

Proceed to `PRV-010` independent revalidation. Do not requeue or execute `PRD-TASK-022` until that passes.
