# PTP-001 Product Task Timeout Policy Remediation

Runtime product execution remained stopped. I did not requeue or execute `PRD-TASK-003`, did not execute any other product task, did not approve another gate, did not resolve `PRD-DEC-002` or `PRD-DEC-003`, and did not reuse the dirty PRE-010 timeout worktree.

## Result

- PTP-001 result: `TIMEOUT_POLICY_REMEDIATED`
- Old timeout: `900s`
- New qualifying timeout: `1800s`
- Standard task timeout: still `900s`

The remediation adds a finite profile-based `ExecutionTimeoutPolicy`. A task receives the high-complexity timeout only when runtime task binding metadata indicates `risk_level = HIGH`, a high-risk model profile, or `verification_profile = FULL_REGRESSION_SECURITY`. Standard tasks remain on the 900 second budget.

## Rationale

`PRD-TASK-003` is a `HIGH` risk task using `MDL-001:Codex executor model:high-risk-guarded` and `FULL_REGRESSION_SECURITY`. PTR-001 showed Codex was actively working until the 900 second wall-clock timeout and had not reached candidate freeze or verification. The preserved worktree contains coherent in-scope changes across the importer, runtime handoff, E2E proof, and deterministic tests, so the narrowest fix is a bounded timeout policy adjustment rather than decomposition.

The new timeout is still finite and enforced. It does not disable the timeout or alter scheduler batch limits, concurrency, failure limits, or repair limits.

## Frozen Plan Compatibility

Timeout is treated as runtime execution-policy metadata, not frozen task identity material. The frozen product plan was not modified.

- Task fingerprint before: `a488123b9f661a3bb36ad8a2cb6013cec831acde8cd9eb3e6a7ebdb3267de6f6`
- Task fingerprint after: `a488123b9f661a3bb36ad8a2cb6013cec831acde8cd9eb3e6a7ebdb3267de6f6`
- Fingerprint impact: none
- Plan reacceptance required: no

## Lease And Executor Behavior

- Standard timeout: `900s`; claimed lease renewal is `960s`.
- High-complexity timeout: `1800s`; claimed lease renewal is `1860s`.
- Guarded-run initial scheduler lease now uses the policy maximum and becomes `3600s`.
- `subprocess.run(..., timeout=...)` remains the executor timeout mechanism.
- Timeout remains terminalized as `executor_timeout`.
- Existing bounded-run persistence tests still prove terminal timeout/failure state survives stop evaluation.

## Attempt 1 Preservation

The PRE-010 attempt remains unchanged:

- Execution: `execution-85a060fff90a4b7d90e44d78778704dd`
- Attempt: `1`
- Status: `timeout`
- Candidate: none
- Commit: none
- Lease: released
- Worktree: `/home/user/projects/.ai_ent-worktrees/PRD-TASK-003/execution-85a060fff90a4b7d90e44d78778704dd`

The worktree is historical evidence only and must not be reused as attempt 2 input.

## Verification

- Targeted timeout/scheduler/executor tests: `31 passed`
- Guarded runner and bounded scheduler integration tests: `10 passed, 1 skipped`
- Preflight: PASS
- Full pytest: `492 passed, 6 skipped in 64.04s`
- Ruff: PASS
- Pyright: PASS
- Alembic: `0020 (head)`

## Runtime State

- `PRD-TASK-003`: blocked
- Gate: `GATE-PRD-EXTERNAL-RUNTIME` approved
- Completed product tasks: `5/25`
- Active leases: `0`
- Nonterminal executions: `0`
- READY tasks: none
- Pending product gates: `7`
- `PRD-DEC-002`: unresolved
- `PRD-DEC-003`: unresolved
- PostgreSQL authority: active

## Recommendation

Run a separate `PTPV-001` revalidation gate. If accepted, requeue `PRD-TASK-003` for a fresh authoritative attempt 2 with the adjusted per-task executor timeout.
